/* 
    - THE INTERFACE FUNCTIONS WOULD BE HERE,
    - THE STORAGE FUNCTIONS WOULD BE HERE,
    - THIS WOULD ALL BE MOVED TO THE MAIN APPLICATION FOR COMPUTATION.
*/

// Parameters.
const the_main_wrapper = document.querySelector('.main-wrapper-for-contents');
const the_form = document.querySelector('form');

const level_of_students_input = document.querySelector('.level-of-students')
const dpt_of_students_input = document.querySelector('.department-of-students')
const no_of_courses_input = document.querySelector('.number')

const no_of_courses_btn = document.querySelector('for-courses')
const no_of_students_btn = document.querySelector('for-students')

const generic_table_courses = document.querySelector('.modal-for-courses')
const generic_table_students = document.querySelector('.modal-for-students')

// For record
const records_btn = document.querySelector('.saved_records')

// Functions.
let create_table = () => {    
    
}

let create_courses_table = (input_value) => {
}

let create_students_table = () => {
}

let store_record_courses = () => {
}

let store_record_students = () => {
}

// Calling the functions.
